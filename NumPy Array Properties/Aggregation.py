import numpy as np

arr = np.array([10, 20, 30, 40, 50])

print(np.sum(arr))    # 150
print(np.mean(arr))   # 30.0
print(np.min(arr))    # 10
print(np.max(arr))    # 50
print(np.std(arr))    # Standard deviation
print(np.prod(arr))   # All values product